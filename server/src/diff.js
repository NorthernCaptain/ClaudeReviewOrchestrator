/**
 * Copyright AlpineReplay Inc, 2026. All rights reserved.
 * Author: Leo Khramov
 */

// TODO(phase-2): build the review payload (git diff HEAD + untracked,
// non-ignored files), enforce maxPayloadBytes / maxFileBytes / maxFiles,
// apply ignorePaths globs, force-include prior-finding files. Emit
// promptHash (exact stdin bytes) and progressHash (promptHash +
// sha256 of every full prior-finding file).

export {}
