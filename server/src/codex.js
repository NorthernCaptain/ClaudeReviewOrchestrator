/**
 * Copyright AlpineReplay Inc, 2026. All rights reserved.
 * Author: Leo Khramov
 */

// TODO(phase-3): spawn `codex exec --cd <repoRoot> --ephemeral
// --sandbox read-only --output-schema <path> -` with the server-built
// prompt on stdin. Validate stdout against codex-output.schema.json via
// ajv; on schema failure return ESCALATE. Drop findings whose `file`
// is not in the current payload (record in droppedFindings).

export {}
