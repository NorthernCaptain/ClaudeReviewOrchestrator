/**
 * Copyright AlpineReplay Inc, 2026. All rights reserved.
 * Author: Leo Khramov
 */

// TODO(phase-6): MCP HTTP transport handler mounted at /mcp. Exposes
// two tools: request_review({ cwd, scope?, extra_instructions? }) and
// reset_review_context({ cwd }). Honors MCP roots/list when the client
// advertises them (extra check on top of allowedRoots).

export {}
