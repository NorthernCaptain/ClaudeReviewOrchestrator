/**
 * Copyright AlpineReplay Inc, 2026. All rights reserved.
 * Author: Leo Khramov
 */

// TODO(phase-2): resolve cwd -> (repoRoot, branch) via `git -C <cwd>`.
// allowedRoots containment check uses fs.realpathSync + path.relative
// boundary validation (no naive prefix).

export {}
