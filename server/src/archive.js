/**
 * Copyright AlpineReplay Inc, 2026. All rights reserved.
 * Author: Leo Khramov
 */

// TODO(phase-4): write <reviewsDir>/<repo>:<branch>/<ts>.json and
// <ts>.md atomically (tmp + rename). Render markdown grouped by
// severity with blocking vs informational split. Skip on cache hits
// and no-progress short-circuits. Retention pruning on startup.

export {}
